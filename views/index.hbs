<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Caesar Cypher</title>
</head>
<script>
         <!--
            function encryptFunction() {
               //alert("Demo Alert");
               const Http = new XMLHttpRequest();
               var data = encodeURIComponent( document.getElementById("dataToEncrypt").value);
               alert(data);
               alert(decodeURIComponent(data));
                const url='/encrypt/'+data;
                Http.open("GET", url);
                Http.send();

Http.onreadystatechange = (e) => {
          
    //alert(Http.responseText);
    let response=JSON.parse(Http.responseText)
    document.getElementById("encryptedData").value= response.result.encryptedData;
  
}
            }

            function decryptFunction() {
               //alert("Demo Alert");
               const Http = new XMLHttpRequest();
               var data = encodeURIComponent(document.getElementById("dataToDecrypt").value);
               //alert(data);
                const url='/decrypt/'+data;
                Http.open("GET", url);
                Http.send();

Http.onreadystatechange = (e) => {
          
    //alert(Http.responseText);
    let response=JSON.parse(Http.responseText)
    document.getElementById("decryptedData").value= response.result.decryptedData;
  
}
            }
         //-->
      </script>
<body>
  
    <h1>Encrypt and Decrypt using Caesar Cypher</h1>

    <table>
        <tr>
            <td><input type="text" value="Here is data" id="dataToEncrypt"></td>
            <td> <input type="text" id="encryptedData"></td>
            <td><button onclick="encryptFunction()">Encrypt</button></td>
        </tr>
        <td><input type="text" value="Type here" id="dataToDecrypt"></td>
            <td> <input type="text" id="decryptedData">></td>
            <td><button onclick="decryptFunction()">Decrypt</button></td>
        </tr>
    </table>
    
   
    
</body>

</html>